

import 'package:flutter/material.dart';

class OneOnOneDateView extends StatelessWidget {
  const OneOnOneDateView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("1-on-1's"),
      ),
    );
  }
}